from itables import init_notebook_mode, show
init_notebook_mode(all_interactive=True)
from itables import options
options.length_menu = [5, 10, 25, 50, 100]
options.buttons = ["copy", "csv", "excel"]
